const config = {
  // 启动端口
  port: 3000,

  // 数据库配置
  database: {
    DATABASE: 'data001',
    USERNAME: 'root',
    PASSWORD: 'wxy613ok',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config